配列の要素を一つずつ取り出して真偽を評価する

.filter()


.filter()
```
{arrayOjb}.filter({条件式や関数})
//戻り値
//配列の要素を一つずつ取り出して、条件の結果がtrueなら残し、falseなら取り除かれる
//全てfalseの場合は空の配列が返される
```

```
{stringObj} ---> 文字列のオブジェクト 
{arrayOjb} ---> 配列のオブジェクト 
{searchValue}　---> 検索する文字列 
{fromIndex}　---> 検索する最初の位置 
{regExp}　---> 正規表現
```
