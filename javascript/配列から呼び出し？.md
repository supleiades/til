
リストから要素を呼び出すときの挙動確認

```
var arr = ["banana","apple","orange"];
var i = 1;
console.log(arr[i]); // apple
var i = a.toString()
console.log(arr[i]); // undefined
var i = parseInt(i,10)
console.log(arr[i]); // apple
```
