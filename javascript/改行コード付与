ブラウザ等に表示されている（改行含む）文字列を
そのまま、エディターで使用したい場合

例) 改行を含んだ文字列の整形を行いたいとき
```
※vim
%s/\r//g
// \r(^M)を削除する
%s/\n/\\n/
// 不可視の改行コードを文字列の「\n」に一時的に置き換える

※エディター(js)
var strCode = {改行コードを文字列の\nに変換した1行を変数に加える}
var arrCode = strCode.split(\n)
```
